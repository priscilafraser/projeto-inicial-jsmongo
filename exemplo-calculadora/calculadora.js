const nome = 'Calculadora do Thi Code!';

const soma = (a, b) => a + b;  //recebo a,b e retorna a+b

const sub = (a, b) => a - b;

const mult = (a, b) => a * b;

const div = (a, b) => a / b;

module.exports = {         //exporta variavel, função
    nome,
    soma,
    sub,
    mult,
    div
};  